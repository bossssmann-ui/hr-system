import { describe, test } from 'bun:test'

describe.skip('finalizeDomesticStage4 composite score isolation', () => {
  test('requires the domestic-stage-scoring module, which is intentionally deleted in prod', () => {})
})
