# Optional Docker Compose overrides for local PostgreSQL.
# Defaults already match README.md and backend/.env.example.

POSTGRES_PORT=54329

# Test runners set a repository-derived POSTGRES_TEST_PORT automatically so
# parallel checkouts do not collide. Uncomment only when you need a fixed manual
# port for docker compose up -d postgres_test.
# POSTGRES_TEST_PORT=54330
