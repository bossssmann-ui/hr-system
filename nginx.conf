server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown routes return index.html so TanStack Router handles them
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Disable access logs for health-check noise reduction
    access_log off;
}
